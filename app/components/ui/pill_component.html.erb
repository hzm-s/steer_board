<%= tag.div class: %w(flex justify-center items-center h-[1.5rem] min-w-[1.5rem] rounded-full bg-slate-200 text-stone-600 text-sm font-bold) + (@wide ? %w(px-3 gap-1) : []) do %>
  <%= content %>
<% end -%>
