<%= link_to new_item_path, data: { turbo_frame: '_self' }, class: %w(block text-center py-1 rounded-lg bg-gray-50 border-2 border-dashed border-gray-400 hover:bg-violet-500 hover:text-white transition duration-150) do -%>
  <%= render IconComponent.new(name: 'plus', size: 1, class: 'text-green-600') %>
  Add new item
<% end -%>
