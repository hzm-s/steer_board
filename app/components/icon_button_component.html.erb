<%= tag.button(
  type: :button,
  data: data.merge(data_for_dropdown),
  class: %w(inline-flex justify-center items-center text-gray-700 bg-white hover:bg-gray-500 hover:text-gray-100),
  style: "width: #{size}rem; height: #{size}rem;"
) do -%>
  <%= content %>
<% end -%>
