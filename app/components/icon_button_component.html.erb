<%= tag.button(
  type: :button,
  data: data.merge(data_for_dropdown),
  class: %w(inline-flex justify-center items-center rounded-lg text-gray-700 hover:bg-white),
  style: "width: #{size}rem; height: #{size}rem;"
) do -%>
  <%= content %>
<% end -%>
