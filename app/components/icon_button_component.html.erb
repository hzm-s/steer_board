<%= tag.button(
  type: :button,
  data: data.merge(data_for_dropdown),
  class: %w(inline-flex justify-center items-center hover:bg-gray-300),
  style: "width: #{size}rem; height: #{size}rem;"
) do -%>
  <%= content %>
<% end -%>
