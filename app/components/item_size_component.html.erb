<% size_classes = {
  sm: %w(h-[1rem] px-2 text-xs),
  lg: %w(h-[1.5rem] px-4),
} %>
<%= tag.span @size, class: %w(inline-block rounded-sm bg-stone-400 text-white font-bold) + size_classes[@element_size] %>
