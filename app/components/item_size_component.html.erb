<% size_classes = {
  sm: %w(px-2 text-xs),
  lg: %w(px-4 text-lg),
} %>
<%= tag.span @size, class: %w(flex items-center rounded-sm bg-stone-400 text-white font-bold) + size_classes[@element_size] %>
