<%= link_to new_item_path, data: { turbo_frame: '_self' }, class: %w(block text-center py-1 rounded-lg bg-gray-200 hover:bg-violet-500 hover:text-white) do -%>
  <%= render Ui::IconComponent.new(name: 'plus', size: 1, class: %w(text-green-600)) %>
  Add new item
<% end -%>
