<%= link_to(
  new_step_path,
  data: { turbo_frame: 'step_form' },
  class: %w(flex-shrink-0 basis-[15rem] link flex gap-2 items-center justify-center rounded-lg border-2 border-dashed border-gray-300 bg-white hover:bg-violet-500 hover:text-white)
) do %>
  <%= render Ui::IconComponent.new(name: 'plus', size: 1, class: %w(text-green-600)) %>
  <%= tag.span 'Add new step', class: %w(block text-sm font-bold) %>
<% end -%>
