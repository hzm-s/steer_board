<%= tag.div(
  data: {
    controller: 'sort',
    action: 'sort:itemStatusChanged->modal#open',
    state_id: @state_id,
    sort_group: 'dummy',
    sort_allow_src: 'dummy',
  },
  class: %w(board__item-list-outer flex flex-col gap-2)
) do %>
  <%= render Kanban::ItemComponent.with_collection(@items) %>
<% end -%>
